<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Generate TOTP Secret</title>
    <link rel="stylesheet" href="styles.css">
    <link rel="icon" href="logo2.png" type="image/png">
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
        }

        .navbar {
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 10px 20px;
            background-color: #f8f8f8;
        }

        form#generate-secret-form {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: flex-start;
            margin: 50px auto; /* Add space between the navbar and the form */
            padding: 20px;
            width: 90%; /* Use a percentage for flexibility */
            max-width: 400px; /* Set a maximum width for larger screens */
            border: 1px solid #ccc;
            border-radius: 10px;
            background-color: #f9f9f9;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        }

        input#username {
            width: 100%; /* Use full width of the form */
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 5px;
            box-sizing: border-box; /* Include padding and border in width */
        }

        label {
            margin-bottom: 10px;
            font-weight: bold;
            text-align: center;
        }

        button {
            margin-top: 10px;
            padding: 10px 15px;
            background-color: #4CAF50;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            width: 100%; /* Make the button span full width */
            box-sizing: border-box; /* Include padding and border in width */
        }

        button:hover {
            background-color: #45a049;
        }

        /* Response container styling */
        #response-container {
            margin-top: 20px;
            width: 100%; /* Ensure it fits within the form */
            word-wrap: break-word; /* Ensure long content wraps to fit within the container */
            text-align: center;
            font-size: 14px;
        }

        @media (max-width: 600px) {
            form#generate-secret-form {
                padding: 15px;
                max-width: 100%; /* Allow form to occupy the entire width */
            }

            button {
                padding: 8px 10px;
                font-size: 14px;
            }

            input#username {
                padding: 8px;
                font-size: 14px;
            }
        }

        @media (max-width: 400px) {
            button {
                font-size: 12px;
                padding: 6px 8px;
            }

            input#username {
                padding: 6px;
                font-size: 12px;
            }
        }
    </style>
</head>
<body>
    <header class="navbar">
        <a class="logo" href='/admin'>
            <img src="logo2.png" alt="Logo">
            <div class="logo" style="text-decoration: none;">
                <span style="color: #333;">TURTLE SHELTER<br>PROJECT</span>
            </div>
        </a>
        <nav class="nav-links">
            <form action="/logout" method="POST" style="display: inline;">
                <a href="#" onclick="this.closest('form').submit();" style="color: rgb(212, 19, 19); text-decoration: none; padding: 10px; cursor: pointer;">Logout</a>
            </form>
        </nav>
    </header>

    <main>
        <form id="generate-secret-form" method="POST">
            <label for="username">Username:</label>
            <input type="text" id="username" name="username" required>
            <button type="submit">Fetch Secret</button>
        
            <div id="response-container">
                <label>Authenticator Key:</label>
                <br>
                <%= result.secret %>
            </div>
        </form>  
    </main>
</body>
</html>