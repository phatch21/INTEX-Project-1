<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Records</title>
    <link rel="stylesheet" href="styles.css">
</head>
<body>
<h1 style="text-align: center;">Admin Records</h1>
<table>
    <thead>
        <tr>
            <th>First Name</th>
            <th>Last Name</th>
            <th>Username</th>
            <th>Modify</th>
        </tr>
    </thead>
    <tbody>
        <% if (admins.length > 0) { %>
            <% admins.forEach(row => { %>
            <tr>
                <input type="hidden" value="<%= row.admin_id %>">
                <td><%= row.fname %></td>
                <td><%= row.lname %></td>
                <td><%= row.username %></td>
                <td>
                    <form action="/editAdmin/<%= row.admin_id %>" method="GET" style="display:inline;">
                        <button type="submit">Edit</button>
                    </form>
                    <form action="/deleteAdmin/<%= row.admin_id %>" method="POST" style="display:inline;">
                        <button type="submit" onclick="return confirm('Are you sure you want to delete this record?');">Delete</button>
                    </form>
                </td>
            </tr>
            <% }) %>
        <% } else { %>
            <tr>
                <td colspan="4">No data available</td>
            </tr>
        <% } %>
    </tbody>
</table>

<form action="/createAdmin" method="GET" style="text-align: center;">
    <button type="submit">Add Admin</button>
</form>

</body>
</html>

